services:
  - type: web
    name: app-web
    env: node
    branch: main
    plan: starter
    instanceType: starter
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
